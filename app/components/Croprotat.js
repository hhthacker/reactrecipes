var ReactDOM = require('react-dom');
var React = require('react');

function Croprotat () {
        return <p1>Let's expand the scope - crops for next year? Woop grids. </p1>;
}

ReactDOM.render(<Croprotat />, document.getElementById('croprotat'))